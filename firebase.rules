rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /nft-images/{allPaths=**} {
      allow read: if true; // Public access to images (for display)
      allow write: if request.auth != null; // Only authenticated users can upload
    }
  }
}
